{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run",
            "type": "cargo",
            "command": "run",
            "problemMatcher": ["$rust-panic"],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "cwd": "${workspaceFolder}/kernel"
            }
        }
    ]
}